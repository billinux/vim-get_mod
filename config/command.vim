"set shell=/bin/bash\ --rcfile\ ~/.bashrc\ -i    " Use Bourne shell for command substitution
set shell=/bin/bash\ -i
set history=1000                    " keep 1000 lines of command line history
set confirm                         " Ask user before aborting an action
